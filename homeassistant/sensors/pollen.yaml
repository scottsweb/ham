platform: rest
name: Pollen Count
resource: !secret pollen_api
scan_interval: 3600
value_template: >
  {%- for day in value_json.forecast -%}
    {%- if now().date() | string() in day.date -%}
      {{ day.pollen_count }}
    {%- endif -%}
  {%- endfor -%}
