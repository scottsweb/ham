alias: Notify Call
initial_state: true
trigger:
  platform: state
  entity_id: sensor.last_call
action:
  - service: notify.all
    data:
      message: "{{ states.sensor.last_call.state }}"
      title: 'Sycamore: Incoming Call'