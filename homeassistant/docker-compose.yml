version: '2'
services:
    homeassistant:
        image: lroguet/rpi-home-assistant:0.73.0
        volumes:
            - "/home/pilot/.ssh:/root/.ssh:ro"
            - "/media/data/Containers/homeassistant:/config"
            - "/etc/localtime:/etc/localtime:ro"
            - "/media/data/Containers/ncid/cidcall.log:/var/log/cidcall.log"
        network_mode: "host"
        restart: always
        container_name: homeassistant
